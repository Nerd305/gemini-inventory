# Copy to mobile/.env (git-ignored). Expo inlines EXPO_PUBLIC_* variables at bundle time.
# Same Gemini key the web app uses (GEMINI_API_KEY in the root .env.local / AI Studio secrets).
EXPO_PUBLIC_GEMINI_API_KEY=
